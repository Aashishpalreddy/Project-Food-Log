{
  "name": "foodlog-app",
  "version": "1.0.0",
  "description": "Simple Food & Symptom Logger",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client:dev": "cd client && npm install && npm run dev",
    "client:build": "cd client && npm install && npm run build",
    "dev": "concurrently \"npm run start\" \"cd client && npm run dev\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "nodemailer": "^6.9.8"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
